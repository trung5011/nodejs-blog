h3 Contact Form
.container
	form(action='/contact/sendmail', method="POST")
		label(for='fname') First Name
		input#fname(type='text', name='firstname', placeholder='Your name..', class="form-contact")
		label(for='lname') Last Name
		input#lname(type='text', name='lastname', placeholder='Your last name..' ,class="form-contact")
		label(for='email') Email
		input#email(type='email', name='email', placeholder='Email',class="form-contact")
		label(for='subject') Subject
		textarea#subject(name='subject', placeholder='Write something..', style='height:200px;',class="form-contact")
		input(type='submit', value='Submit')
style.
	body {font-family: Arial, Helvetica, sans-serif;}
	* {box-sizing: border-box;}
	input.form-contact, select.form-contact, textarea.form-contact {
	width: 100%;
	padding: 12px;
	border: 1px solid #ccc;
	border-radius: 4px;
	box-sizing: border-box;
	margin-top: 6px;
	margin-bottom: 16px;
	resize: vertical;
	}
	input[type=submit] {
	background-color: #4CAF50;
	color: white;
	padding: 12px 20px;
	border: none;
	border-radius: 4px;
	cursor: pointer;
	}
	input[type=submit]:hover {
	background-color: #45a049;
	}
	.container {
	}

